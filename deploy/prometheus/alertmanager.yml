route:
  receiver: default
  group_by: [alertname, severity]
  group_wait: 30s
  repeat_interval: 4h
receivers:
  - name: default
    # Configure webhook/email/PagerDuty in a private deployment overlay.
